<h1><%= @category.name %></h1>

<h2>Recipes in this Category</h2>

<ul>
  <% @category.recipes.each do |recipe| %>
    <li>
      <%= recipe.name %> |
      <%= link_to "Delete", category_bookmark_path(@category, @category.bookmarks.find_by(recipe: recipe)), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %>
    </li>
  <% end %>
</ul>

<%= link_to "Add a Recipe to this Category", new_category_bookmark_path(@category), class: "btn btn-primary" %>

<br>
<%= link_to "Back to Categories", categories_path %>
